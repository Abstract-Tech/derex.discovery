version: "3.5"

services:
  lms:
    networks:
      derex:
        aliases:
          - discovery-complete.localhost

  cms:
    networks:
      derex:
        aliases:
          - studio.discovery-complete.localhost

  ecommerce:
    networks:
      derex:
        aliases:
          - ecommerce.discovery-complete.localhost
